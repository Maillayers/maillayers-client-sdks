{
  "name": "@maillayers/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Internal framework-neutral protocol, license, and lifecycle implementation shared by the MailLayers client SDKs. Never published; bundled into each framework package.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint.mjs",
    "test": "node scripts/test-controller.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.3"
  }
}
